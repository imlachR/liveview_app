const Profile = {
  mounted() {
    // var formAvatar = this.el;
    // var formInput = formAvatar.getElementsByTagName("input")[1];
    // var formLabel = formAvatar.getElementsByTagName("label")[0];
    // formLabel.htmlFor = formInput.id;
  },

  updated() {
    // var formAvatar = this.el;
    // var formInput = formAvatar.getElementsByTagName("input")[1];
    // var formLabel = formAvatar.getElementsByTagName("label")[0];
    // formLabel.htmlFor = formInput.id
  }
}

export default Profile
